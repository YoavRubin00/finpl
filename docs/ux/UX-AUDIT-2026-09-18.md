# אודיט UX מלא — FinPlay (ענף dev, dc3cd564) — 2026-09-18

**מחבר:** יפיופי (UX + Game Designer)
**היקף:** מסכי הטאבים, זרימת מתחיל, נגישות, RTL, ביצועים נתפסים, כלים פיננסיים, תיק המניות החי.
**שיטה:** סריקה סטטית של הקוד + 4 סריקות-משנה ממוקדות (נגישות/RTL, זרימת-מתחיל, כלים+תיק, ביצועים+מסכים משניים). כל ממצא עם file:line שנפתח ואומת. לא נכלל מה שכבר טופל ב-8107b658 (18.8) וב-a2707919 (9.9).
**עובדת-יסוד ל-RTL:** `app/_layout.tsx:20-23` כופה LTR (`forceRTL(false)`) — לכן `row-reverse` הוא הדפוס הנכון לשורה עברית, `ChevronLeft` = "קדימה", `→` = "אחורה".
**תקציב-מסך ב-iPhone SE (375×667):** status 20 + GlobalWealthHeader ≈ 63 + tab bar ≈ 85 → **~500px** לתוכן בטאב.

> כל שינוי מוצר (IA, קופי, זרימה) עובר דרך ים לפני ביצוע. תיקוני נגישות/עקביות/באגים טהורים — לא.

---

## Top-12 לביצוע מיידי (ROI = השפעה ÷ (מאמץ × סיכון))

| # | מזהה | תמצית | מאמץ | עדיפות |
|---|---|---|---|---|
| 1 | UX-01 | פתיחה-קרה נוחתת על "מרכז ההשקעות" — למשתמש מתחת ל-400 XP זה מסך נעול, אפור, בלי CTA | S | P0 |
| 2 | UX-50 | ~13 שניות של טיימרים כפויים לפני התשובה הראשונה: 3×1.15s בפרופיל (מסך-1 = הדליפה הגדולה), 5s סטריק בלי CTA נראה, פופאפ-קוויז שנעלם לבד אחרי 2s ומפיל הקשה על תשובה | S | P0 |
| 3 | UX-31 | תיק המניות: שדה מחיר-קנייה עם `$` קבוע — מניית ת"א שנקנתה ב-₪45 מציגה רווח של ~+9,900% | M | P0 |
| 4 | UX-51 | פריימר ההתראות יכול להיערם מעל תיבת mod-0-1 (רגע התגמול הראשון) — הסלוט מבוסס-זמן ואף משטח-תיבה לא תופס אותו | S | P1 |
| 5 | UX-02 | InvestmentsHubScreen ו-Simulator (בטאב) בלי ScrollView — נחתכים ב-SE ובכל הגדלת-גופן | S | P1 |
| 6 | UX-21 | Flash-of-zero: ההדר מציג 0 מטבעות/יהלומים/רצף בכל פתיחה קרה; וגם תגמול תיבת-הפתיחה נזקף מאחורי רקע אטום | M | P1 |
| 7 | UX-15 | תיק המניות: אין טעינה/שגיאה/"עודכן לפני"/pull-to-refresh; נייר בלי ציטוט נעלם מהסכום; ההאב לא סופר אחזקות חיות | M | P1 |
| 8 | UX-32 | טאב-בר: 4 טאבים לא-פעילים ב-#94a3b8 (3.0:1, נכשל AA) + `allowFontScaling={false}` ×25 | S | P1 |
| 9 | UX-07 | 6 "כחולים ראשיים" ל-CTA בלי טוקן primary; 4 קונבנציות-חץ ו-8 ניסוחי "המשך" לאורך הזרימה הראשונה | M | P1 |
| 10 | UX-04 | גלולת רווח/הפסד: כפתור-מזויף, "+-120" בהפסד, חץ הפוך | S | P1 |
| 11 | UX-33 | הכלים שכולם רואים (ריבית-דריבית, FIRE) הם היחידים בלי "מה עכשיו"; "כלי היום" לא מופיע בהאב | S | P1 |
| 12 | UX-09 | Lottie בלולאה בלי gating על מסכי-טאב שנשארים mounted: MoreScreen ×11, Chat header, DuoLearn ×2, אש-הרצף בהדר | S | P1 |

**13-15 (כמעט נכנסו):** UX-14 בורר-באנרים במפה (M) · UX-17 חגיגת המניה הראשונה (S) · UX-52 מיקרו-XP לכל תשובה נכונה (S).

---

## A. מסכי הטאבים

### UX-01 — פתיחה-קרה נוחתת על מסך השקעות נעול (P0, S)
- **איפה:** `app/(tabs)/_layout.tsx:24` `initialRouteName="investments"`; `app/index.tsx:4` `<Redirect href="/(tabs)" />`; `app/_layout.tsx:806-809` `router.replace("/(tabs)")` למשתמש חוזר.
- **הבעיה:** `src/features/investments/InvestmentsHubScreen.tsx:38-39` — הטאב פתוח רק ב-`layer >= 2` או לפני ה-walkthrough. שכבה 2 = 400 XP (`src/constants/economy.ts:11,18`), מודול ≈ 50 XP (`LessonFlowScreen.tsx:6257`) → ~6 מודולים. משתמשי D1-D7 פותחים את האפליקציה אל מסך ב-`opacity: 0.55` (`:106`), "נפתח בשלב 2", בלי כפתור.
- **למה זה משנה:** כלל 3-השניות של Brawl Stars — הערך הראשון שרואים הוא "נעול". שני workarounds בקוד כבר מעידים: `src/components/ui/BackButton.tsx:27-30`, `app/tower-defense-boss.tsx:18-20`.
- **תיקון:** `initialRouteName="index"` + מחיקת שני ה-workarounds. אם ההחלטה (970c281e, אפריל) נשארת — לפחות CTA "ממשיכים ללמוד" → `/(tabs)/index` במסך הנעול. (אישור ים.)

### UX-02 — פריסות ללא גלילה נחתכות ב-SE (P1, S)
- `InvestmentsHubScreen.tsx:138` — `View` עם `flex: 1`, אין ScrollView (grep = 0). מחשבון: כותרת 26 + באנר שארק 140 (`:363-371`) + כרטיס-תיק 122 (`:447-451`) + 3 כרטיסי ניווט × 70 + gaps 50 + paddings 26 ≈ **574px** מול ~500 זמינים → "הנכסים שלי" מתחת לטאב-בר. ב-iPhone 13 נשארים ~47px — כל הגדלת-גופן חותכת.
- `src/features/simulator/SimulatorScreen.tsx:237-292` — מסך 1 הוא `flex:1, justifyContent:"space-between"` בלי גלילה (שורת-חזרה 44 + פין 70 + כרטיס-גרף + 3 סליידרים ~62 + כפתור) ≈ 580px; הנתיב `/(tabs)/simulator` (מ-`StockAnalystScreen.tsx:476`) מרנדר תחת ההדר והטאב-בר.
- **תיקון:** ScrollView עם `contentContainerStyle` בשני המסכים; ב-Investments להסיר `flex: 1` מ-`s.content`.

### UX-03 — מסך הנעילה: ניגודיות ומבוי סתום (P1, S)
- `InvestmentsHubScreen.tsx:106` `opacity: 0.55` על כל הבלוק; `:124` `#cbd5e1` על `#f8fafc` ≈ 1.5:1 — נכשל AA ותקנה 5568. → `#64748b`, אטימות רק על ה-Lottie, ו-SupercellButton "ממשיכים ללמוד".

### UX-04 — גלולת רווח/הפסד (P1, S)
- `InvestmentsHubScreen.tsx:181-186`: (א) `Pressable accessibilityRole="text"` בלי `onPress`; (ב) `+{Math.round(totalPnl)}` → "+-120"; (ג) `→` = אחורה ב-RTL.
  ```tsx
  const pnl = Math.round(totalPnl);
  <Pressable onPress={() => router.push("/trading-hub")} accessibilityRole="button" accessibilityLabel={`רווח והפסד ${pnl}, פתיחת זירת המסחר`}>
    <Text style={[s.pnlText, { color: pnl >= 0 ? "#15803d" : "#dc2626" }]}>{pnl >= 0 ? "+" : ""}{pnl} רווח/הפסד</Text>
    <ChevronLeft size={14} color="#64748b" />
  </Pressable>
  ```

### UX-05 — "תיק ההשקעות שלך" = יתרת מטבעות (P1, S)
- `InvestmentsHubScreen.tsx:176-179` — הכותרת "תיק ההשקעות שלך", הערך `coins`. מאז 9.9 יש "תיק המניות שלי" אמיתי ב-₪. שני "תיקים" עם שמות כמעט זהים. → "המטבעות שלך למסחר" / "התיק הווירטואלי".

### UX-18 — תיק המניות האמיתי לא נגיש מ-Investments Hub (P1, S — אישור ים)
- `InvestmentsHubScreen.tsx:202-262` — 3 כרטיסים (מסחר / זירת נכסים / הנכסים שלי). `HoldingsSection` מותקן רק ב-`NetWorthDashboardScreen.tsx:115`, שנגיש מטאב "כלים" (`FinancialToolsScreen.tsx:129-132`). → כרטיס רביעי "תיק המניות שלי (אמיתי)" → `/net-worth-dashboard`, subtitle חי: `holdings.length ? formatShekel(totalIls) : "מעקב חי אחרי מניות שבאמת יש לכם"`.

### UX-34 — שברון בצד הלא-נכון בכרטיסי הניווט (P2, XS)
- `InvestmentsHubScreen.tsx:207-208, 238-239, 252-253` — `ChevronLeft` הוא הילד **הראשון** ב-`row-reverse` (`:457`) → מתרנדר בקצה **הימני**, ותיבת-האייקון בשמאל. ב-`FinancialToolsScreen.tsx:218-235` הסדר הפוך (אייקון ימין, שברון שמאל) — שני האבים לא עקביים. → להעביר את `<ChevronLeft>` להיות הילד האחרון.

### UX-06 — קופי בהאב ההשקעות מפר BRAND.md (P2, S)
- `:161` "בנה את האימפריה הפיננסית שלך" (זכר יחיד; מערכת = רבים) → "בונים אימפריה פיננסית". `:292-296` 3 אמוג'ים כתבליטים → אייקוני lucide. `:312` "הבנתי, קח אותי לזירה!" → "הבנתי, לזירה".

### UX-07 — אין טוקן primary אחד ל-CTA (P1, M)
- `InvestmentsHubScreen.tsx:307` `#3b82f6→#2563eb`; `FinancialToolsScreen.tsx:242` `FB_BLUE '#1877f2'`; `AnimatedTabBar.tsx:61` `#0891b2`; `DuoLearnScreen.tsx:3208` `#0ea5e9`; `src/constants/theme.ts:85` `CALM.accent '#0891b2'`, `:110` `DUO.blue '#005bb1'`, `:154` `STITCH.primary '#005bb1'`; `CalculateButton.tsx:115-122` `#005bb1`. 25 זוגות-גרדיאנט שונים ב-CTA-ים; `SupercellButton` ב-9 קבצי-פיצ'ר בלבד.
- בחנות: `ShopItemCard.tsx:297-312` (radius 12, ירוק, border-bottom 3), `ShopScreen.tsx:1090-1095` exchangeBtn (radius 10, טקסט שחור), `:1039` priceBtn (radius 12 בלי border) — 3 סגנונות "קנה".
- **תיקון:** `BRAND.primary` יחיד (מומלץ `#0891b2` — כבר בטאב-בר וב-CALM); `SupercellButton variant="primary"` לכל CTA ראשי; `ShopCtaButton` אחד. מיגרציה: Investments → Tools → Register-CTA → Shop.

### UX-21 — Flash-of-zero בפתיחה קרה (P1, M)
- `GlobalWealthHeader.tsx:201-205` `coins = economyData?.coins ?? 0`; `useEconomy.ts:15-20` `useQuery` בלי `placeholderData`; `src/lib/queryClient.ts` בלי persistence. אותו דפוס ב-`ProfileScreen.tsx:134-135`, `ShopScreen.tsx:312-319` (כפתורי-קנייה נראים רגע "אין מספיק").
- **למה זה משנה:** הדבר הראשון שכל משתמש רואה בכל פתיחה הוא "0 מטבעות" ואז count-up. Hay Day לא מציג 0 — מציג את הערך האחרון.
- **תיקון:** snapshot אחרון של economy ב-AsyncStorage → `placeholderData`; או shimmer-pill בזמן `isPending`.

### UX-35 — כפילות safe-area תחתונה (P2, S — לאמת במכשיר)
- `ChatScreen.tsx:1061` `SafeAreaView edges={['bottom']}` ו-`InvestmentsHubScreen.tsx:138` `paddingBottom: insets.bottom + 16` — בתוך Tabs שהטאב-בר שלהן כבר צורך `insets.bottom` (`AnimatedTabBar.tsx:253`). ב-iPhone 15 ≈ 34px מתים מעל הטאב-בר.

### UX-36 — חנות: מדף "פרימיום" ריק לצמיתות + ספלאש 1.5 שניות (P1, S)
- `shopItems.ts:226-232` חושף 5 קטגוריות; ספירה: avatars 10 / boosts 4 / hearts 2 / protection 5 / **premium 0** → "אין פריטים בקטגוריה זו" (`ShopScreen.tsx:589-593`) תמיד, בלי שארק ובלי CTA. → `SHOP_CATEGORIES.filter(c => SHOP_ITEMS.some(i => i.category === c.key))`.
- `ShopScreen.tsx:340-343, 490` — ספלאש 1.5 שניות בכל כניסה עם RN `Image` מ-Blob בלי cache → בפעם הראשונה לבן. → להסיר או `ExpoImage.prefetch` ולהציג רק כשב-cache.
- `ShopScreen.tsx:150-175` `DiamondPattern` = 42 Views × 6 = 252 Views דקורטיביים בכל mount → תבנית SVG אחת.

### UX-37 — פרופיל: "טרם נשאל" כערך סטטיסטי + עובדות מתחת ל-5 כרטיסי-המרה (P2, S/M)
- `ProfileScreen.tsx:522-523` — הצ'יפים מציגים "טרם נשאל" (מצב פנימי) כערך. → להסתיר או "הוסיפו".
- סדר (`:222-592`): register-CTA → פילים → באנר-שם עם קונפטי בכל פתיחה (`:257-259`) → זהות → רצף → Bridge → שותף → נכסים → הזמנה → **הפרופיל** → אוסף → AI → Pro. עובדות הפרופיל אחרי 5 כרטיסי-המרה. (החלטת מוצר.)
- אין מחיקת-חשבון בפרופיל וב-More (`MoreScreen.tsx:222-341`) — לוודא ב-Settings לפני סקירת-חנות.

### UX-38 — Fantasy "בקרוב" ומבוי-סתום (P2, S)
- `FantasyComingSoon.tsx:31-51` — חזרה + "בקרוב" + 2 שורות, בלי פעולה. מגיעים אליו מה-hero המוצמד ב-`FriendsHubScreen.tsx:144-148` וב-`:117`. → CTA "בינתיים: אתגר היום" ו/או "עדכנו אותי"; ולהוריד את ה-hero כל עוד `FANTASY_COMING_SOON`. גם ההערה ב-`app/(tabs)/_layout.tsx:41-42` ("gated/blocked") לא תואמת למציאות — הפיד מלא.

### UX-12 — טאב-בר: `isNew` מת + תוויות שנשברות (P2, XS)
- `AnimatedTabBar.tsx:52` `isNew: true` — `:157` שולף רק `{ Icon, label, badge }`; מעולם לא מוצג. `:219-228` תווית בלי `numberOfLines={1}`/`maxFontSizeMultiplier` → "השקעות" נשבר לשתי שורות ב-Dynamic Type ≥1.6×. `:265` `return null` בזמן מקלדת → remount + spring בכל שליחה בצ'אט (→ להנפיש גובה במקום unmount).

### UX-20 — צפיפות GlobalWealthHeader (P2, S — מוצר)
- `GlobalWealthHeader.tsx:445-620` — 6 לחיצים בשורה; מטבעות ויהלומים שניהם → חנות. → להסתיר גלולת יהלומים כש-`gems === 0` (רוב המתחילים). כפתור הגדרות `:446-453` יורד ל-28pt ב-SE בלי `hitSlop` → `hitSlop={8}`.

---

## B. זרימת מתחיל (onboarding → walkthrough → mod-0-1 → chest → מפה)

**חוזקות (לא לגעת):** `successHaptic`/`errorHaptic` על תשובות (`LessonFlowScreen.tsx:1252,1258`); XP ומטבעות עפים (`FlyingRewards` `:6877-6887`); prefetch תמונות+אודיו לאינטרו (`:2506`); כפתורי-תשובה עם role/label/state (`:1418-1431`); ProfilingFlow 39 תוויות על 28 Pressables; הצ'אט נפתח עם ברכה (`ChatScreen.tsx:784-804`), לא ריק.

### UX-08 — Reduce Motion לא מכובד ברוב הלולאות (P1, S)
- `src/utils/animations.ts:105-140` — `useEntranceAnimation` לא קורא `useReducedMotion` (בשימוש ב-5 אלמנטים ב-InvestmentsHub `:96-100` ועוד).
- `withRepeat(-1)` בלי gate: `AnimatedTabBar.tsx:101-114` (walkthroughPulse — ה-hook כבר קיים ב-`:94`), `GlobalWealthHeader.tsx:353-360, 381-388`, `ProfileScreen.tsx:181-188`, `ChatScreen.tsx:638-644`, `LessonFlowScreen.tsx:4125-4136` (3 לולאות על התיבה), `StreakCelebrationScreen.tsx:122-131` (+וידאו loop `:85`), `ProfilingFlow.tsx:403-410` (sharkWobble ב-StepShell) — ו-LessonFlowScreen (6,939 שורות)/OnboardingScreen/ChestCelebrationModal/SignupGateStep בלי אף התייחסות.
- בזרימה הראשונה: `ProfilingFlow.tsx:189, :245` — `LottieIcon autoPlay loop` על **כל** כרטיס-אפשרות = 4-5 לולאות במקביל בכל שאלת-פרופיל (`LottieIcon.tsx:27` בלי reduced-motion); `ModuleIntroShort.tsx:296-301, 326-331, 343-348` — 3 לולאות (הקובץ בודק reduce-motion רק לכיתוב/חץ `:110-127`).
- **תיקון:** `const rm = useReducedMotion(); progress.value = rm ? 1 : withDelay(...)`; לפני כל `withRepeat`: `if (rm) { v.value = static; return; }` (הדפוס הקיים ב-`DuoLearnScreen.tsx:526-530`); ל-Lottie `autoPlay={!rm} loop={!rm}`.

### UX-14 — ראש מפת הלמידה בלי בורר-באנרים (P1, M)
- `DuoLearnScreen.tsx:3229` EnergyStationCard (מוצמד), `:3240` TomorrowChestCard, `:3045-3056` FirstLessonCTA + StreakAtRisk + NoFreezeUpsell + ToolsDiscovery + ProPromoExpiring — כל אחד עם self-gating; מנגנון slot קיים רק ל-3 (`BridgeCTABanner`, `ToolsDiscoveryBanner`, `ProPromoExpiringBanner`).
- **למה זה משנה:** FRONT-DECLUTTER (11.7): 2,900 dismissals מול 3 פעולות. הצומת הזהוב חייב להיות מעל הקפל.
- **תיקון:** `useBannerSlot` יחיד עם עדיפות (StreakAtRisk > FirstLesson > NoFreeze > ToolsDiscovery > ProPromo); מקסימום באנר אחד + אנרגיה. חוק: הצומת הפעיל נראה ב-iPhone 13 בלי גלילה.

### מפת-הצעדים: מפתיחה עד התשובה הראשונה (control arm, אורח)
| # | מסך | file:line | פעולה | המתנה כפויה |
|---|---|---|---|---|
| 1 | Welcome | `ProfilingFlow.tsx:2080-2170`, קופי `banditConfig.ts:362-384` | "מתחילים" | CTA נכנס ב-scale מ-0 אחרי 200ms (`:1980,:1985`) |
| 2-4 | חלום / מטרה / גיל | `:814`, `:943`, `:1041` | הקשה על כרטיס | **1150ms auto-advance** כל אחד (`AUTO_ADVANCE_MS :72`; `:834, :951, :1053`) |
| 5 | Signup gate | `SignupGateStep.tsx:38` | "המשך כאורח" (`:242-260`) | — |
| 6 | Celebration | `ProfilingFlow.tsx:620` | "מתחילים" (`:745`) | CTA אחרי 700ms (`:644`) + 400ms (`:656`) |
| 7 | סטריק יום-1 | `StreakCelebrationScreen.tsx` | הקשה בכל מקום (`:279`) או auto 5s (`:186-189`) | עד **5s** |
| 8 | תיבת-פתיחה | `PostWalkthroughFirstChest.tsx:163` | 2 הקשות (`:184`, `:230`) | — |
| 9-16 | סיור, 8 צעדים | `AppWalkthroughOverlay.tsx:46-121` | 8 הקשות (או "דלג" `:535`) | 350ms ניווט לכל צעד (`:356-368`) |
| 17 | אינטרו mod-0-1 | `LessonFlowScreen.tsx:5192-5266` | "מתחילים ←" (`:5241-5263`) | SharkLoader עד 1500ms (`:2515`) |
| 18-20 | 3 פלאשקארדים, 12 segments | `chapter0Data.ts:14-64` | ~12 הקשות "המשך" | — |
| 21 | QuizStartPopup | `QuizStartPopup.tsx:34-37` | "בואו נתחיל!" או auto 2s | עד **2s** |
| 22 | תשובה ראשונה | `LessonFlowScreen.tsx:1244` | הקשה | — |

**סה"כ: ~22 מסכים, ~30 הקשות, ~13 שניות טיימרים כפויים.** מול Brawl Stars (90 שניות למשחק ראשון) — הסיור (8 מסכים) הוא הבלוק הגדול, ה-segments השני. קופי: אין בלוק >120 תווים (הארוך = 104, `AppWalkthroughOverlay.tsx:66`).

### UX-50 — טיימרים כפויים בזרימה הראשונה (P0, S)
- **(א)** `ProfilingFlow.tsx:72` `AUTO_ADVANCE_MS = 1150` × 3 מסכים = 3.45s "מת" בדיוק במסך-1 (הדליפה הגדולה, −18.6% לפי memory). → ~500ms, או הקשה שנייה על הכרטיס הנבחר = קידום מיידי (`advancedRef` כבר קיים).
- **(ב)** `StreakCelebrationScreen.tsx:276-279, :186-189` — כל המסך Pressable אבל אין רמז; הטקסט היחיד "מחר: יום 2 ברצף"; auto-dismiss 5s. שתי חגיגות ברצף (Celebration → Streak, `ProfilingFlow.tsx:2990-2998`) לפני שום ערך. → "המשך" קטן, ולשקול איחוד לתוך Celebration.
- **(ג)** `QuizStartPopup.tsx:34-37` — `onStart` אוטומטי אחרי 2000ms **וגם** כפתור "בואו נתחיל!" (`:99`). הקשה שכוונה לכפתור אחרי unmount נוחתת על אפשרות בקוויז שמאחוריו (`LessonFlowScreen.tsx:5376`) = תשובה בטעות. → לבטל את ה-auto-dismiss (או את הפופאפ ל-mod-0-1).
- **(ד)** `ProfilingFlow.tsx:1980, :1996` — CTA ה-Welcome מאנימט מ-`scale 0`; הקוד עצמו מתעד ב-`:624-629` שהדפוס משאיר hit-rect קרוס ולא-לחיץ בחלק מגרסאות Reanimated. → opacity כמו ב-CelebrationScreen (`:644`).

### UX-51 — פריימר ההתראות נערם מעל תיבת mod-0-1 (P1, S)
- `TopicTreeAccordion.tsx:484` `markCompleted('mod-0-1')` באותו effect שפותח את התיבה (`:523`). `NotificationPermissionPrompt.tsx:102-107` — `eligible` נדלק על `completedIds.includes('mod-0-1')` ובודק רק `pendingFirstChest/ProTeaser/RegisterCTA`; אחרי 1200ms (`:150`) `canTakePopupSlot()` (`:129`) — סלוט מבוסס-זמן ש-`ChestCelebrationModal`/`TopicTreeAccordion`/`ModuleEndSignupGate` לא תופסים (`takePopupSlot` רק ב-BridgeCTABanner, Day0ExitRitualHost, StarterCapitalGrantHost). שני RN `<Modal>` (`NotificationPermissionPrompt.tsx:175`, `ChestCelebrationModal.tsx:423`).
- **למה זה משנה:** ה-ask הכי יקר באפליקציה (memory: notification trigger = שורש הרטנשן) נוחת על רגע התגמול הראשון. → `!chestVisible` ב-`eligible`, או `takePopupSlot()` בפתיחת ChestCelebrationModal ו-ModuleEndSignupGate.

### UX-53 — Modal-over-Modal ב-handoff תיבה→סיור (P1, S)
- `PostWalkthroughFirstChest.tsx:145` `clearFlag(false)` + `:149` `triggerWalkthrough()` + `:159` `router.replace('/(tabs)')` באותו tick — בדיוק הדפוס ש-`AppWalkthroughOverlay.tsx:342-345` מתעד כמפיל את ה-Modal השקוף ב-iOS. → `triggerWalkthrough` ב-`requestAnimationFrame`, ולדלג על replace כשכבר ב-/(tabs) (`isAlreadyOnRoute` `:322-330`).

### UX-54 — Hidden state: תגמול תיבת-הפתיחה לא נראה (P1, S/M)
- `PostWalkthroughFirstChest.tsx:107-108` `addCoins/addXP` מאחורי backdrop אטום `#0f172a` (`:274`) שמסתיר את ההדר; הסיור שאחריו לא מצביע על הארנק. הפעם הראשונה ש"מרוויחים" — בלי לראות יתרה. → `FlyingRewards` לכיוון ההדר במפה אחרי "המשך", או backdrop חצי-שקוף אחרי הפתיחה.

### UX-52 — אין מיקרו-תגמול לתשובה נכונה (P1, S)
- יש: `successHaptic` (`LessonFlowScreen.tsx:1252`), feedback bar (`:1456-1478`), קומבו (`:4434-4468`). XP/מטבעות רק בתיבה (`TopicTreeAccordion.tsx:490-491`). `FirstLessonCTA` הבטיח "מתחילים לצבור מטבעות ו-XP" — ואין סימן עד סוף המודול; ב-mod-0-1 קוויז יחיד (`chapter0Data.ts:65-80`) → גם פופאפ-הסטריק (≥3) לא יכול להופיע.
- **Hay Day standard:** כל פעולה = מטבע עף. → `FlyingRewards type="xp"` (+5) לכיוון ההדר על תשובה נכונה (הרכיב כבר מחובר `:6877`). (מוני מאשר את הערך.)

### UX-55 — אינטרו mod-0-1: כיתובים לפי שעון ולא לפי אודיו (P1, S/M)
- `ModuleIntroShort.tsx:26` `PHASE_DURATIONS 4000/4800`, `:100-105` טיימר שלא תלוי ב-`audioState`; `useIntroAudio` ממתין לקובץ (`:62`), "טוען אודיו..." רק ב-`slow` (`:219-223`). ברשת איטית הכיתוב מתחלף כשהשארק עוד לא מדבר — הרגע הראשון של הקול נראה שבור. → להתחיל את טיימר הפאזות רק ב-`audioState === 'playing'` (או `failed`).

### UX-56 — Signup gate: שני קירות ו-4 כפתורים באותו משקל (P1/P2, S/M)
- `SignupGateStep.tsx:160` Apple שחור, `:194` Google `#0ea5e9`, `:227` Email `#0ea5e9` (**זהה**), `:253` אורח אפור — אין היררכיה, נראה כטופס ברגע ה-peak-commitment. → Google ראשי, Email outline, אורח לינק. (S)
- שני signup walls בסשן הראשון: `ProfilingFlow.tsx:3086-3092` לפני כל ערך + `ModuleEndSignupGate` (`TopicTreeAccordion.tsx:869-878, :904-925`) מיד אחרי התיבה. → להשאיר את הפוסט-chest, הראשון inline לא-חוסם. (M, ים.)

### UX-57 — הסיור: 8 צעדים עם ניווט-טאב לפני השיעור הראשון (P1, M — ים)
- `AppWalkthroughOverlay.tsx:46-121`; צעדים 4-8 (חברים/כלים/צ'אט/חנות/גשר) מציגים פיצ'רים שלא נגישים למשתמש עם 0 מטבעות. 8 מתוך ~22 המסכים. → 3 צעדים (מסלול / תיבה+אנרגיה / חברים), השאר tooltips בביקור הראשון בכל טאב.

### UX-58 — תוויות CTA לא עקביות לאורך הזרימה (P1, S)
- אותה פעולה, שני שמות: "המשך בלי חשבון" (`ProfilingFlow.tsx:2262`) מול "המשך כאורח" (`SignupGateStep.tsx:259`).
- התחלה: "בואו נתחיל!" (`QuizStartPopup.tsx:99`), "בואו נתחיל" (`FirstLessonCTA.tsx:115`), "יאללה לקוויז!" (`LessonFlowScreen.tsx:993`), "הכל ברור, קדימה! ✓" (`:6629`), "מתחילים" (×3).
- סיור: "יאללה, קדימה!" / "מה נלמד באפליקציה?" / "עכשיו לחברים" / "המשך" / "לחנות" / "ומה עם הגשר?" (`AppWalkthroughOverlay.tsx:52-117`) — 3 דקדוקים.
- חיצים: "המשך >" ASCII = **אחורה ב-RTL** (`LessonFlowScreen.tsx:1491`), "מתחילים ←" (`:5263`), "דלג ›" (`ModuleIntroShort.tsx:164`), אייקון (`AppWalkthroughOverlay.tsx:524`) — 4 קונבנציות.
- מגדר (BRAND: קול השארק = יחיד ניטרלי): "מוכן לאתגר?"/"בדוק את עצמך" (`QuizStartPopup.tsx:70,78` זכר), "אתה מבזבז" (`LessonFlowScreen.tsx:6139`), "מוכנים לשיעור הראשון?" (`FirstLessonCTA.tsx:91` רבים), chest "שלך" (יחיד).
- a11y ≠ טקסט: `ProfilingFlow.tsx:745` label "בואו נתחיל" על כפתור "מתחילים".
- **תיקון:** "המשך" לכל קידום-בתוך-זרימה, "מתחילים" רק להתחלות, אייקון `ChevronLeft` אחד, יחיד-ניטרלי בקול השארק.

### UX-59 — תשובות הפרופיל אובדות ב-kill (P2, S)
- `collected` = React state בלבד (`ProfilingFlow.tsx:2762-2795`), נשמר רק ב-signup gate (`:3094-3120`). → לכתוב ל-`useTutorialStore` בכל `slide()`.

### UX-60 — פלאשקארד ראשון בלי skeleton (P2, S)
- SharkLoader מוגבל ל-1500ms (`LessonFlowScreen.tsx:2515`) ואז הכרטיס נפתח גם אם `imagesReady=false` (`:2516-2520`, ההערה `:2508-2515` מודה ב-pop-in). קופסה לבנה בכרטיס הראשון של משתמש חדש. → blurhash/shimmer בגובה קבוע.

### UX-19 — ניווט אוטומטי בהדרכה (P2, S)
- `InvestmentsHubScreen.tsx:56-62` — צעד 3 דוחף ל-`/trading-hub` אחרי 2 שניות בלי מגע ("hidden state"). → glow על הכרטיס ו-push רק בלחיצה.

### UX-26 — Register-CTA במפה (P2, XS)
- `DuoLearnScreen.tsx:3179` "מדלגים קדימה? 🚀" (אמוג'י בכותרת), `:3218` "המשך כאורח" (זכר יחיד, הודעת-מערכת) → "ממשיכים כאורחים".

**נבדק ותקין:** Android back בסיור חוזר צעד (`:557`); "דלג" בכל צעד; אנרגיה כבויה ב-mod-0-1 (`useHeartsStore.ts:31`); "המשך בלי אודיו" בכשל (`ModuleIntroShort.tsx:224-236`); DoN מושתק ביום הראשון (`ChestCelebrationModal.tsx:268-275`); back-out מהפרופיל שומר בחירות. קוד מת: sub-step `"register"` ב-IntroStep (`ProfilingFlow.tsx:2172-2290`) לא נגיש.

---

## C. נגישות (תקנה 5568 / WCAG 2.1 AA)

### UX-32 — ניגודיות טקסט אפור (P1, S)
- `AnimatedTabBar.tsx:62,222,346` `TAB_INACTIVE = "#94a3b8"` — 4 טאבים לא-פעילים (11px) על לבן = 3.0:1. → `#64748b` (4.8:1).
- `InvestmentsHubScreen.tsx:124` (`#cbd5e1` + opacity), `:568` "אל תראו לי הסבר זה שוב" `#94a3b8`; `DuoLearnScreen.tsx:3649,3656` subtitle פרק נעול; `LessonFlowScreen.tsx:6771` "ביטול ספירה לאחור". → `#64748b`.
- תקינים: `#6b7280` (4.8:1), `#94a3b8` על כרטיסים כהים (LessonFlow `:651, :6849`).

### UX-23 — allowFontScaling={false} ×25 (P1, S)
- DuoLearn 6 / Profile 6 / Shop 5 / Chat 1 / LessonFlow 1. הבעייתיים באמת: `ProfileScreen.tsx:232,235,240` (register-CTA), `ShopScreen.tsx:282,283,293` (**מחיר החבילה**), `LessonFlowScreen.tsx:5850` (CTA "המשך"), `DuoLearnScreen.tsx:3271`. הדפוס הנכון קיים: `FriendsHubScreen.tsx:104` `maxFontSizeMultiplier={1.15}`. → `maxFontSizeMultiplier={1.3}` (1.15 ל-pills).

### UX-10 — תוויות/hitSlop חסרים (P2, XS כל אחד)
- `DuoLearnScreen.tsx:3474-3479` X של מודל-הריפליי (38×38) בלי role/label/hitSlop; `:3499` CTA "צפה שוב" בלי label.
- `InvestmentsHubScreen.tsx:274-275` backdrop (סוגר) ו-sheet בלי role; → backdrop `accessibilityLabel="סגור"`, sheet `accessible={false}` (כמו `DuoLearn:3175`).
- `GlobalWealthHeader.tsx:698` card-wrapper עם `onPress={() => {}}` בלי `accessible={false}` → כל תוכן הפופאפ נבלע לאלמנט אחד.
- `LessonFlowScreen.tsx:4944-4954` "חזור" (~30pt רוחב) → `hitSlop={{left:12,right:12}}`.
- `MoneySlider.tsx:99-133` בלי `accessibilityValue` (Compound `:371` ו-FIRE `:689` עושים נכון).
- `HoldingRow.tsx:60` — התווית = שם+שווי; שינוי-יומי ורווח לא מוקראים.

**כיסוי תוויות (Pressables / labels):** Investments 8/6 · DuoLearn 24/21 · Tools 2/2 · Chat 10/10 · Profile 19/17 · Shop 6/6 · TabBar 1/1 · Header 7/6 · LessonFlow 75/70.

---

## D. RTL

- הדפוס הגלובלי עקבי בטאבים (Investments 0 `row` / 6 `row-reverse`, Tools 0/6, FriendsHub 0/1). MoreScreen (`:377,423,435`) `row` בסדר-LTR ידני — עובד, שביר, לא באג.
- **UX-39 — חיצים/שברונים הפוכים (P2, XS):** `InvestmentsHubScreen.tsx:185` `→`; `LessonFlowScreen.tsx:6816, 6854` "ממשיכים ללמוד →" (מול `:6710` "קח את ההטבה ←" — לא עקבי), `:1833` "→ תיבות שלך"; `:4707-4711` "המשך מאיפה שהפסקתי" + `ChevronLeft` בתוך `row` → השברון מימין לטקסט; `:4690` אותו דבר. → `←` / `row-reverse`.
- **UX-40 — צ'אט: כפתור-שליחה מימין (P2, XS — עיצוב):** `ChatScreen.tsx:1209, 1506-1508` `inputStyles.bar` הוא `row` → שלח מימין ל-TextInput (וואטסאפ עברית = שמאל). `:1424-1427` metaRow (שעה) בשמאל הבועה. לאשר מול העיצוב או `row-reverse`.
- UX-34 (שברונים בכרטיסי ההשקעות) — ראו למעלה.
- `textAlign:'right'` כמעט לא בשימוש (Investments/DuoLearn/Chat/LessonFlow = 0); פסקאות עבריות מסתמכות על `writingDirection` בלבד — ב-forced-LTR שורה שנייה של פסקה בלי `textAlign` תיצמד לשמאל. לבדוק `DuoLearnScreen.tsx:1031-1033` `modalBody`.

---

## E. ביצועים נתפסים

### UX-09 — Lottie בלולאה בלי gating על מסכים שנשארים mounted (P1, S)
- `freezeOnBlur` (`app/(tabs)/_layout.tsx:24-33`) מקפיא JS — **לא** Lottie נייטיבי. מסכים רשומים ב-Tabs (גם `href: null`) נשארים mounted.
- `MoreScreen.tsx:119, 222, 238, 243, 248, 261, 291, 296, 321, 338` — **11** `autoPlay loop` בלי gate; More הוא route של Tabs → אחרי ביקור אחד רץ תחת כל טאב.
- `ChatScreen.tsx:1082` header Lottie בלי `active`; `DuoLearnScreen.tsx:961` (מטבע easter-egg), `:1252` (רקטה "דלג לפרק 1"); `ModuleSharkCallCard.tsx:87` (PRO badge); `GlobalWealthHeader.tsx:491` אש-הרצף (SOFTWARE render, `LottieIcon.tsx:62`) רצה תמיד.
- לולאות בתוכן-שיעור בלי gate (נזק רק כשמודל פתוח מעליהן): `BarterPuzzleScreen`, `InflationThiefScreen`, `WhatIsMoneyIntro`, `CompoundInterestIntro`, `FlashcardInfographic`, `InteractiveIntroCard`, `ModuleIntroShort`, `FIRECalcScreen`.
- **תיקון:** הדפוס כבר קיים — `ProLockBadgeLottie` (`DuoLearnScreen.tsx:38-49`), `LottieIcon active={isFocused}` (Shop `:614`), `{isFocused && <LottieView/>}` (Investments `:109`). לאש-הרצף: `loop={false}` + `burstEveryMs` כמו הקונפטי (`:586-595`).

### UX-41 — תמונות מרוחקות בלי placeholder/transition/cache (P2, S)
- `InteractiveIntroCard.tsx:184` (תמונת-אינטרו של שיעור — מלבן ריק קופץ באמצע שיעור), `ShopItemCard.tsx:98`, `DailyDealsSection.tsx:197`, `DailyNewsChallengeCard.tsx:171` (RN Image), `DuoLearnScreen.tsx:3486` (כרטיס-סיכום בריפליי, RN Image), `AvatarImage.tsx:46` (cache יש, placeholder אין). → `cachePolicy="memory-disk" transition={180}` + blurhash כחול-מותג. דוגמאות טובות בריפו: `FlashcardInfographic.tsx:740`, `CarouselSheet.tsx:126`, `living-lesson/visuals/ImageVisual.tsx:35-40`.

### UX-13 — אין Skeleton/EmptyState משותפים (P2, S)
- `src/components/ui/` בלי `Skeleton*`; ה-shimmer היחיד הוא `ShimmerBar` פרטי ב-`SharkLoader.tsx:38`. `Ta35ForecastCard.tsx:80-84` ו-`CrowdWisdomCard.tsx:174-179` עושים fetch בלי שום loading UI. → לחלץ `Skeleton.tsx` (reduced-motion aware) + `EmptyState.tsx` (שארק + כותרת + CTA).

### UX-42 — עבודה כבדה ב-mount (P2, S)
- `DuoLearnScreen.tsx:2473-2481` — rAF + 5 טיימרים (50/130/280/500/800ms) בכל שינוי `topicTreeModule`, למרות ש-`:2452-2464` נוחת בפאס הראשון. → rAF + fallback אחד של 140ms.
- `SimulatorScreen.tsx:133-147` — `setInterval` 80ms × 3 שניות = 37 re-renders של גרף Skia. → `withTiming` אחד.
- `DuoLearnScreen.tsx:3231→3293→1302` — 6 פרקים × 43 מודולים ב-ScrollView אחד (שלב-שני דרך `runAfterInteractions` `:1612-1621`). → mount עצל לפי `onScroll` (כבר קיים `:3236-3240`) לפרקים מעבר ל-active+1.

---

## F. כלים פיננסיים (src/features/financial-tools)

**עובדה:** 4 הכלים ב-`tools/` (שכר-נטו, החזר-מס, משכנתא, דמי-ניהול) כולם `hidden: true` (`toolsRegistry.ts:172,188,204,220`) — נגישים רק דרך next-step/deep-link. המחשבונים שרואים בפועל (ריבית-דריבית, FIRE) חיים ב-`chapter-1-content/simulations/CompoundSimScreen.tsx` ו-`chapter-5-content/simulations/FIRECalcScreen.tsx` ולא משתמשים ב-atoms.

### UX-33 — "מה עכשיו" חסר בדיוק בכלים שרואים (P1, S)
- `financialNextStepMap.ts:76,83` מגדיר `compound` ו-`fire`, אבל `ToolNextStepCard` מותקן רק ב-4 הכלים המוסתרים (Salary `:282`, Tax `:302`, Mortgage `:425`, Pension `:440`) וב-Payslip (`:413`). ב-Compound/FIRE — 0 imports. → `<ToolNextStepCard toolKey="compound"|"fire" />` במסך התוצאה.
- `toolOfTheDay` מוזן לבאנר-הבית ול-CTA בשיעור (`ToolsDiscoveryBanner.tsx:58`, `SharkCTAModals.tsx:273`) אבל **לא להאב** — 8 כרטיסים שטוחים בלי היררכיה. → שורת "כלי היום" מעל הרשתות.
- `HeroStatStrip` (`FinancialToolsScreen.tsx:119-122`) קורא `useNetWorthStore.totalValue()` = נכסים ידניים בלבד → משתמש עם אחזקות חיות בלבד רואה "+ הוסיפו נכסים" (ראו UX-15).

### UX-43 — עקביות atoms ו-CTA (P2, M)
- atoms (`components/atoms/index.ts`) בשימוש ב-4 הכלים; Compound (סליידר/סטפר/כפתור משלו `:218, :478-523, :874` radius 20) ו-FIRE (`:77-81, :677, :1029` radius 20) לא. רדיוסים: atoms 16 / hub+holdings 12 / Compound+FIRE 20. כפתורי-שיתוף: ירוק `#22c55e` (Salary `:246`), אינדיגו `#6366f1` (Mortgage `:411`), כתום `#fb923c` (Tax `:293`), ורוד `#ec4899` (Pension `:433`).

### UX-44 — תוצאות בלי "למה" (P2, S)
- `MortgageCalculator.tsx:185-210` — "נראה אפשרי / שווה לבדוק שוב" בלי סיבה (יחס-החזר? הון-עצמי?); הסיבה קיימת רק כרמז 90px למטה (`:293`). → `יחס החזר ${pct1(incomeRatio)} · הון עצמי ${pct1(downPaymentRatio)}`.
- `TaxRefundCalculator.tsx:154-167` — "החזר משוער" בלי "שילמת X · לפי המדרגות Y".
- טובים: Salary `:170-176`, Pension `:364-374`.

### UX-45 — קלט ומקלדת (P2, S/M)
- ניקוי שדה-ברוטו → hero "₪0 · 0.0% מס" (Salary `:170-176`) — רק Tax מגדר עם `hasInput` (`:125,155`). → להעתיק.
- סליידר קופץ למינימום כשהשדה ריק (Salary `:144-148`, Mortgage `:134-136`, Tax `:128-132`).
- 4 הכלים: `SafeAreaView edges={['top']}` + ScrollView `paddingBottom 80`, **בלי KeyboardAvoidingView** (Salary `:160-164`, Tax `:144-148`, Mortgage `:174-178`, Pension `:221-226`); רק Pension עם `keyboardShouldPersistTaps` → ב-3 האחרים "חשב" עם מקלדת פתוחה = 2 הקשות; שני השדות האחרונים ב-Tax (`:206, :243`) מתחת למקלדת ב-SE. Payslip כבר עם KAV (`:321`) → להעתיק.
- **Pension:** ה-hero מתרנדר **מתחת** לכפתור "חשב" (`:363` אחרי `:348`) אבל `handleCalculate` גולל ל-`y:0` (`:180`) — הלחיצה מרחיקה מהתוצאה.
- טעויות-כתיב: `SalaryNetCalculator.tsx:260` "לעסיק" → "למעסיק"; `:278` "לעיר שכיר" → "לעומת שכיר".
- `FinancialToolsScreen.tsx:340,347,372` — `fontSize: 10` (מתחת למינימום 11-12). `:104` FinTip "כל כלי שתסיים… פותחת לך" (זכר+נקבה מעורבבים) → "כל כלי שמסיימים נותן XP — סיימתם את כולם? נפתחת מערכת פיננסית מלאה." `:163` "+ הוסיפו נכסים" ב-24px זהב נראה כערך, לא CTA.

---

## G. תיק המניות החי (holdings/) — הצעות קונקרטיות

**מה כבר טוב:** מצב-ריק כן עם CTA (`HoldingsSection.tsx:126-147`), שינוי-יומי צבוע (`:150-176`), המלצת-שיעור לפי צורת התיק (`:132-150`) — הדיפרנציאטור האמיתי, קטלוג 46 ניירות כולל 15 מניות ת"א עם שמות עבריים (`tickerCatalog.ts:70-84`), חיפוש עברי (`AddHoldingModal.tsx:252-266`), `decimal-pad` (`:69`), תוויות נגישות.

### UX-31 — מניות ת"א: שדה "$" ורווח פי-100 (P0, M)
- `holdingsCatalog.ts:24` `avgBuyPriceUsd`; `AddHoldingModal.tsx:298` `suffix="$"` קבוע; `HoldingRow.tsx:47-50` `pnlPct = (quote.price − avgBuyPriceUsd)/avgBuyPriceUsd` ביחידת הציטוט. ל-15 ה-`.TA` Yahoo מצטט **באגורות** → משתמש שקנה לאומי ב-₪45 רואה "$", מקליד 45, ומקבל ≈ +9,900% "סה"כ".
- **למה זה משנה:** הפיצ'ר בנוי על אמון במספר אמיתי; מספר מופרך = הפיצ'ר נזרק.
- **תיקון:** `suffix={isTaseTicker(t) ? '₪' : '$'}`; שינוי-שם ל-`avgBuyPrice` במטבע הציטוט; חישוב P&L דרך `unitPriceToIls` בשני הצדדים.

### UX-15 — אין מצבי טעינה / שגיאה / רעננות, ואינטגרציה חלקית (P1, M)
- `HoldingsSection.tsx:37` — רק `data`; `isLoading/isError/dataUpdatedAt` לא בשימוש בכל `net-worth-dashboard/`. שורות מציגות "מחיר בטעינה…" (`HoldingRow.tsx:40`) גם בטעינה **וגם לנצח** כשטיקר נכשל (`useHoldingsQuotes.ts:77` משמיט). `:50-51` נייר בלי ציטוט → `continue` → נעלם מ"שווי חי". כרטיס-הסכום מוסתר עד `totalIls > 0` (`:150`) → קפיצת-פריסה.
- `usdIlsLive` (`useHoldingsQuotes.ts:24,82`) לא מוצג; בכשל FX קבוע **3.1** (`:28`) מתמחר את כל התיק בשקט. `refetchOnWindowFocus` (`:88`) לא פעיל ב-RN (`src/lib/queryClient.ts:39-41` בלי `focusManager`); אין `refetchInterval`; אין `RefreshControl` (`NetWorthDashboardScreen.tsx:103-106`).
- אחזקות לא נכנסות ל-`useNetWorthStore` — רק `onValueChange` → `NetWorthDashboardScreen.tsx:45-49` (צמיחה 8% קבועה, לא מוצגת); ההאב (`FinancialToolsScreen.tsx:119`) לא רואה אותן.
- **תיקון:** skeleton לסכום ב-`isLoading`; "אין מחיר כרגע" כש-`data && !quote`; "עודכן HH:MM" + "שער דולר משוער" כש-`!usdIlsLive`; `RefreshControl` → `refetch`; `focusManager.setEventListener(AppState)`; `setHoldingsSnapshot(valueIls, at)` ב-store כדי שההאב יספור.

### UX-17 — ההוספה הראשונה בלי חגיגה (P1, S)
- `AddHoldingModal.tsx:141-163` — `tapHaptic()` וסגירה. ביומן-ההחלטות (11.7) "המהלך הכספי האישי הראשון" = NSM מוצע — זה הרגע. → כש-`holdings.length === 0` לפני שמירה: `successHaptic()` + `FlyingRewards type="xp"` + שורת שארק "יש לנו תיק. מהיום עוקבים ביחד" + `track('first_real_holding_added')`.

### UX-16 — מחיקה/עריכה חבויות (P2, M)
- מחיקה = הקשה על שורה → גלילה מתחת ל-disclaimer → "הסרה מהתיק" (`AddHoldingModal.tsx:306-315`) → `Alert` (`:168`) = 3 הקשות. → `onLongPress` ב-`HoldingRow` → ActionSheet (עריכה/הסרה); כפתור-ההסרה מעל ה-disclaimer; בהמשך Swipeable.

### UX-30 — מודל ההוספה: safe-area ומקלדת כפולה (P2, XS)
- `AddHoldingModal.tsx:189-195` `SafeAreaView` בתוך `Modal presentationStyle="formSheet"` (וגם `AssetEditModal.tsx:159-161`) — מלכודת ה-iOS הידועה; → `useSafeAreaInsets` כמו ב-a2707919.
- `:196-197` `KeyboardAvoidingView behavior=padding` **וגם** `:219` `automaticallyAdjustKeyboardInsets` → over-scroll במיקוד על מחיר-קנייה. להשאיר אחד.
- ולידציה ב-`Alert.alert` (`:134,138`) = מודל-על-מודל → שגיאה inline מתחת לשדה. חיפוש בלי `autoFocus`/`returnKeyType` (`:249-257`).
- מצב-ריק: להוסיף שורה "מניות ארה"ב, ת"א וקריפטו" כדי שידעו את היקום לפני ההקשה.

---

## H. מה מאומת כטוב (לא לגעת)
- Tab bar: `accessibilityRole="tab"` + state + hint, reduced motion על ה-scale, הסתרה בזמן מקלדת.
- Lottie מגודר ב-InvestmentsHub, Shop, TradingHub; ההדר אחרי דיאטת-האנימציות (7.3).
- juice בשיעור (הפטיקה, XP עף, prefetch); כפתורי-תשובה נגישים.
- Chat: ברכה, chips, typing indicator, KAV, כפתור-שליחה עם disabled.
- מצב-הריק והמלצת-השיעור בתיק המניות.

## I. סדר-ביצוע מוצע (שבועיים)
1. **יום 1-2 (S, בלי אישור מוצר — באגים/נגישות/עקביות):** UX-50(ג) auto-dismiss של QuizStartPopup, UX-50(ד) scale-0 CTA, UX-51 סלוט-פופאפ לתיבה, UX-53 handoff תיבה→סיור, UX-04, UX-32, UX-23, UX-10, UX-34, UX-39, UX-12, UX-09 (More/Chat/DuoLearn/הדר), UX-02 (ScrollView ×2), UX-03, UX-58 (חיצים + a11y≠טקסט).
2. **יום 3-5 (M):** UX-31 (ת"א), UX-15 (מצבי-טעינה + store), UX-21 (placeholderData), UX-54 (תגמול נראה), UX-33 (next-step + כלי-היום), UX-08, UX-55 (כיתובים↔אודיו).
3. **דורש ים (מוצר):** UX-01 (initialRoute), UX-50(א,ב) קיצור טיימרים + איחוד חגיגות, UX-57 (סיור 8→3), UX-56 (קיר-הרשמה יחיד), UX-52 (מיקרו-XP — גם מוני), UX-58 (ניסוח CTA אחיד), UX-18 (כרטיס IA), UX-05 (שם), UX-14 (בורר-באנרים), UX-17 (חגיגה), UX-36 (מדף פרימיום), UX-38 (Fantasy hero), UX-20 (יהלומים), UX-37 (סדר פרופיל).
4. **מדידה:** לפני/אחרי — onboarding screen-1→completed (777667), `chest_opened` unique (793300), W1 retention, `first_real_holding_added` (חדש), `wealth_banner_tapped`, notification-permission accept rate.
