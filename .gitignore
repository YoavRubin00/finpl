node_modules
.env
.env.local
.env.*.local
.env.production
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.secrets/
*.orig.*
web-build/
.vercel
.env*.local

# Large media — hosted on CDN (Vercel Blob / R2), not bundled in repo
assets/video/
assets/AUDIO/
assets/vercel_stage/
assets/daisy/
assets/audio-podcasts/
assets/audio-couple-dilemmas/
assets/audio-samples/
assets/couple/
# Daisy animation WebPs — uploaded to Vercel Blob 2026-05-22 and referenced
# by URL from src/features/podcast-segment/daisy-assets.ts. Local files may
# exist on a developer's machine but should never enter the repo or the
# native bundle (the app downloads them at runtime from CloudFront/Blob).
assets/webp/daisy/
# Podcast studio background PNG — same Vercel Blob pipeline as the Daisy
# WebPs above.
assets/images/podcast/
voices-temp.json
voices-hebrew*.json
voices-israeli.json
voices-h*.json
shared-female.json
models.json
# Only track root PNGs referenced by code; ignore draft/archive subfolders
assets/infographics/*/
assets/STOCK/
assets/IMAGES/modules/
assets/images/games/

# Dev/build scripts — local tooling only
scripts/
notebooklm-py/

# Local scratch folders for one-shot asset generation pipelines
# (Higgsfield video → frames → chroma-key → animated WebP). These create
# hundreds of intermediate files; nothing in them should ship.
tmp-*/
tmp-*.png
tmp-*.webp
tmp-*.mp4
/db/
*.ps1
copy_*.js
copy-images.js
copy_images.mjs
organize_vercel_images.js
setup_swipe_images.js
update-summaries.js
update-zoom.js
updateAllSummaries.js
updateFlashcards.js
script_update_pro.js
tasks_output.txt
test.html
scratch/
dev
skills/
review_*.md
AGENT-MAP.md
BRAND_VOICE.md
INFOGRAPHIC_RECIPE.md
PRD_FinPlay_Stich.md
.vercelignore
