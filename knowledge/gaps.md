# ⚠️ gaps — פערים פתוחים שדורשים החלטת יואב

> פערים שמאיה זיהתה בסריקת הצוות (2026-06-15) ו**לא תיקנה לבד** כי הם דורשים מחיקה/שכתוב הרסני, שינוי עובדה, או קבצים מחוץ לתחום ה-MD.
> מאיה תיקנה לבד רק דברים אדיטיביים/הפיכים (בניית המוח, עיגון frontmatter). הפערים הבאים ממתינים להחלטה.

## G1 — `docs/marketing/` חסר (חוסם את בר)
**הבעיה:** הסוכן **בר** מפנה במפורש ל-`docs/marketing/BRAND/` (content-pillars.md ⭐, voice.md, brand-kit.md, soul-ids.md, prompt-library.md) ו-`docs/marketing/CONTENT/` (calendar.md, ideas-bank.md). **התיקייה לא קיימת בפרויקט.** `docs/BRAND.md` מפנה למקבילות ב-`higgsfield-marketing-studio` שמחוץ לפרויקט.
**ההשפעה:** בר בנוי על "5 עמודי התוכן" ובנק-רעיונות שאינם נגישים לו כאן. הוא מתפקד חלקית (קול בלבד מ-`docs/BRAND.md`).
**אופציות ליואב:** (א) להעתיק את `docs/marketing/` מ-higgsfield-marketing-studio לכאן; (ב) לעדכן את בר שיצביע למיקום האמיתי; (ג) לבנות את המסמכים מחדש כאן. **דורש החלטה — לא לבצע בלי אישור.**

## G2 — `MEMORY.md` חסר (מוזכר ע"י 3 סוכנים)
**הבעיה:** `/דואו`, `/יפיופי`, `/רטנשן` מורים "קרא `CLAUDE.md` + `MEMORY.md`". **`MEMORY.md` לא קיים** בשורש הפרויקט.
**ההשפעה:** הוראת הקריאה נכשלת בשקט. הסוכנים עדיין עובדים (CLAUDE.md קיים), אבל מצפים לזיכרון שאינו שם.
**אופציה:** או ליצור `MEMORY.md` קל (יכול להצביע ל-`knowledge/INDEX.md` החדש), או לעדכן את 3 הסוכנים שיצביעו ל-`knowledge/`. **מאיה ממליצה** על האחרון (אדיטיבי) — ממתין לאישור עיגון.

## G3 — yoatzon stale מול ה-OS (צוות + נתונים)
**הבעיה:** `.claude/agents/yoatzon.md` כאן מזכיר את **"איתי" כמפרסם פעיל** ("בעלים: איתי") ונתוני KPI מסוף מאי 2026. לפי [[business-facts]] (ושינוי הצוות יוני 2026) — איתי עזב, "מפרסם" = **יואב**, והנתונים stale.
**גם בר** מזכיר "איתי (מפרסם)" ו"שותף לאסטרטגיה ולפרסום בפועל".
**ההשפעה:** ניתוב משימות מרקטינג עלול ללכת לאדם שעזב.
**אופציה:** עדכון נקודתי של שתי השורות ("איתי" → "יואב (מרקטינג)") — שינוי מהותי בעובדת-צוות בקובץ סוכן פעיל. **מאיה לא דרסה לבד** (גבול: שינוי עובדה בסוכן פעיל). ממתין לאישור — תיקון של 2 שורות.
**הערה:** מקור-האמת לפרופיל יואב/צוות הוא `CLAUDE.md` של ה-OS; המוח הזה מצביע לשם דרך [[business-facts]].

## G4 — אי-אחידות frontmatter בין agents ל-commands
**הבעיה:** סוכני `.claude/agents/` משתמשים ב-`name`/`description`/`model`. סוכני `.claude/commands/` משתמשים ב-`name`/`description`/`argument-hint`/`allowed-tools`/`auto-activate`. זה **תקין** (שני סוגי קבצים שונים), אבל אף אחד מהם לא הכריז על `knowledge:` עד היום.
**מה מאיה עשתה:** הוסיפה שורת עיגון "טען `knowledge/INDEX.md` קודם" + הפניה ל-knowledge בכל סוכן (אדיטיבי, הפיך). לא שינתה את מבנה ה-frontmatter הקיים.

## G5 — שם המסקוט: "קפטן שארק" מול "Finn"
**הבעיה:** ב-`docs/BRAND.md` ובקוד ההתראות מופיע גם השם **"Finn"** (`finnNotificationCopy.ts`, "Finn מרגיש עצוב") לצד "קפטן שארק". `/רטנשן` משתמש ב-"Finn happy → Finn sad".
**ההשפעה:** אי-בהירות אם זו אותה דמות בשני שמות או שתי דמויות.
**אופציה:** יואב להבהיר את הקנון (סביר ש-Finn = השם הישן של אותה דמות). מאיה **לא שינתה** — זו עובדת-מותג. ממתין להבהרה.

---

## מה מאיה כן תיקנה לבד (אדיטיבי/הפיך) — לתיעוד מלא
ראה `audits/agent-evals/finplay-brain-2026-06-15.md` ב-OS. בתמצית: בניית `knowledge/` (INDEX + 6 עוגנים), עיגון 14 הסוכנים ב-frontmatter, התקנת maya. **שום מחיקה, שום דריסה הרסנית.**
