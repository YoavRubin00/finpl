// Deprecated 2026-05-31. DNC sounds now go through `useSoundEffect` hook
// (src/hooks/useSoundEffect.ts) — same pattern as the lesson modules.
// File kept temporarily so a stray import would still type-check; safe to
// delete once a clean grep confirms no callers remain in the repo.
export const __DNC_SOUNDS_DEPRECATED__ = true;
