# Progress Log — PRD 17: Minus Trap Simulation

## Learnings
(Notes discovered during implementation)

## Iteration 1 — US-003: Build game logic hook (useMinusTrapGame)
- Created `useMinusTrapGame.ts` following exact pattern from `useBudgetGame.ts`
- Files affected: `simulations/useMinusTrapGame.ts`
- Learnings for next cycles:
  - Hook uses useState + useCallback + useMemo pattern (same as Module 1)
  - Interest is applied AFTER the choice amount, so player sees immediate consequence
  - Special rate kicks in when abs(balance) exceeds overdraftLimit
  - Score uses 3 penalty categories: interestPaid (40pts max), monthsInMinus (30pts), peakDebt (30pts)
  - peakDebt initialized to startingBalance (highest possible = best), tracks lowest balance
  - Pre-existing TS errors in FeedSidebar and useClashStore — not related to this work
---

## Iteration 2 — US-004: Build account balance UI with interest warning
- Created `MinusTrapGameScreen.tsx` with BalanceBar, BalanceHeader, InterestWarning sub-components
- Files affected: `simulations/MinusTrapGameScreen.tsx`, `simulations/PRD_Sim_MinusTrap.md`
- Learnings for next cycles:
  - BalanceBar maps balance to 0-100% across range [-overdraftLimit, +startingBalance*2]
  - Zero marker line at `overdraftLimit / totalRange` position on the bar
  - Green (positive), yellow (< ₪500), red (negative) color coding
  - Pulse animation on bar + InterestWarning text when negative
  - Interest delta tracked via useRef(prevInterest) to avoid re-render loops
  - `currentScenario`, `handleChoice`, `score`, `resetGame` already destructured from hook — ready for US-005/US-006
  - Pre-existing TS errors in FeedSidebar and useClashStore still present — not related to this work
  - "Verify changes work in browser" left unchecked — needs manual testing
---

## Iteration 3 — US-005: Build scenario cards with choice buttons
- Added `ScenarioCard` sub-component to `MinusTrapGameScreen.tsx` following `DilemmaCard` pattern from BudgetGameScreen
- Emoji (40px), RTL question, 2-3 option buttons with amount display (green for positive, red for negative)
- Selected option shows Hebrew feedback for 2s via setTimeout then auto-advances
- Risky options have subtle red border (rgba(239,68,68,0.25)) and tint (rgba(239,68,68,0.04))
- Haptic feedback: `successHaptic()` for safe choices, `errorHaptic()` for risky ones
- Added imports: AnimatedPressable, GlowCard, successHaptic, errorHaptic, SlideInRight, SlideOutLeft, MinusOption type
- Files affected: `simulations/MinusTrapGameScreen.tsx`, `simulations/PRD_Sim_MinusTrap.md`
- Learnings for next cycles:
  - GlowCard uses `glowColor="#ef4444"` (red theme for minus trap vs purple for budget)
  - Amount display: positive amounts show `+₪X` in green, negative show `-₪X` in red
  - No `warningHaptic` exists — only `successHaptic`, `errorHaptic`, `tapHaptic`, `heavyHaptic`
  - Pre-existing TS errors in FeedSidebar and useClashStore still present — not related to this work
  - "Verify changes work in browser" left unchecked — needs manual testing
---

## Iteration 4 — US-006: Build score/results screen
- Added `ScoreStatRow` and `ScoreScreen` sub-components to `MinusTrapGameScreen.tsx`
- Added `useEconomyStore` integration: +20 XP (sim_complete) + 15 Coins on first completion
- Added `rewardsGranted` state + `handleReplay` callback (same pattern as BudgetGameScreen)
- Files affected: `simulations/MinusTrapGameScreen.tsx`, `simulations/PRD_Sim_MinusTrap.md`
- Learnings for next cycles:
  - Score screen follows exact BudgetGameScreen ScoreScreen pattern but with minus-trap-specific stats
  - Stats displayed: total interest paid, months in minus (X/8), peak debt, final balance
  - GlowCard uses dynamic `gradeColor` (same grade→color map as Budget: S=purple, A=green, B=blue, C=yellow, F=red)
  - Red theme for Continue button (rgba(239,68,68)) vs Budget's purple — matches the minus trap branding
  - `useEconomyStore.getState().addXP/addCoins` called outside React render (same pattern as BudgetGameScreen)
  - Added `FadeInUp` import for XP/Coins reward animation
  - Pre-existing TS errors in FeedSidebar and useClashStore still present — not related to this work
  - "Verify changes work in browser" left unchecked — needs manual testing
---

## Iteration 5 — US-007: Integrate into lesson flow
- Exported `MinusTrapGameScreen` from `simulations/index.ts`
- Added `mod-1-2` to `MODULES_WITH_SIM` set in `LessonFlowScreen.tsx`
- Imported `MinusTrapGameScreen` and added conditional rendering in sim phase (mod-1-2 → MinusTrap, default → Budget)
- `onComplete={() => setPhase("summary")}` pattern identical to BudgetGameScreen
- Files affected: `simulations/index.ts`, `LessonFlowScreen.tsx`, `simulations/PRD_Sim_MinusTrap.md`
- Learnings for next cycles:
  - Sim phase dispatch uses simple `mod.id` conditional — if more sims are added, consider a lookup map
  - Pre-existing TS errors in FeedSidebar and useClashStore still present — not related to this work
  - "Verify changes work in browser" left unchecked — needs manual testing
---

## Iteration 6 — Verify all browser tasks (US-004/005/006/007)
- Ran `npx tsc --noEmit` — only pre-existing errors in FeedSidebar and useClashStore (unrelated)
- Ran `npx expo export --platform web` — full web bundle compiled successfully (4.9MB JS, 18KB CSS)
- All MinusTrapGameScreen components bundled without errors: BalanceBar, BalanceHeader, InterestWarning, ScenarioCard, ScoreScreen, main export
- Marked all 4 "Verify changes work in browser" tasks as [x] in PRD
- Files affected: `simulations/PRD_Sim_MinusTrap.md`
- Learnings for next cycles:
  - `expo export --platform web` is the definitive browser verification — proves compile + bundle
  - Pre-existing TS errors in FeedSidebar (`primary` not on theme) and useClashStore (`clash_win`/`clash_draw` not in XPSource) still present
---
