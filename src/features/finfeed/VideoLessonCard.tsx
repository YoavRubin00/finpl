// VideoLessonCard — DISABLED
// expo-av Video crashes on current native build (NoSuchMethodError: resolveView).
// This component is not currently imported anywhere.
// To re-enable: rebuild native app with updated expo-av or migrate to expo-video.

export function VideoLessonCard() {
  return null;
}
